{
  "name": "asibitget",
  "version": "2.0.0",
  "description": "ASIBitget — WebSocket-based Bitget market data proxy for Cloudflare Workers",
  "main": "src/server.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
